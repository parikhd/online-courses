Array
array of primitive will store primitive
array of objects will store address of mem loc where actual object resides

retrieval by index is O(1)
base addr + (index*length)

rest operations are O(n)
